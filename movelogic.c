void movelogic(void){

int delta = /*0 if user is x, 1 if user*/;

for(i=0;i<9;i++){
        if (delta == 0){
                /*do the stuff*/
                ++delta;
                continue;
        }else if(delta ==1){
                /*do the other stuff*/
                --delta;
                continue;
        }
}
}
